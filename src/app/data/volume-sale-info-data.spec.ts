import { VolumeSaleInfoData } from './volume-sale-info-data';

describe('VolumeSaleInfoData', () => {
  it('should create an instance', () => {
    expect(new VolumeSaleInfoData()).toBeTruthy();
  });
});
