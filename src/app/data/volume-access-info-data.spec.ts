import { VolumeAccessInfoData } from './volume-access-info-data';

describe('VolumeAccessInfoData', () => {
  it('should create an instance', () => {
    expect(new VolumeAccessInfoData()).toBeTruthy();
  });
});
